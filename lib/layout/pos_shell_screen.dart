import 'package:flutter/material.dart';

class PosShellScreen extends StatelessWidget {
  const PosShellScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('POS Desktop')));
  }
}
